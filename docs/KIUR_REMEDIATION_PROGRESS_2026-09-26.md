# حالة تنفيذ متطلبات الأمن والدخول وتجربة المستخدم — 26 سبتمبر 2026

هذا الملف يقيس التنفيذ مقابل `KIUR_SECURITY_AUTH_UX_REQUIREMENTS_2026-09-25.md`. فُحصت إعدادات Cloudflare وحالة D1 والنهايات العامة للموقع؛ ما زالت تجربة الدخول بحساب Google حقيقي وإرسال بريد حقيقي بحاجة إلى تحقق.

| البند | الحالة | الدليل أو العمل المتبقي |
| --- | --- | --- |
| SEC-01 | مكتمل محلياً | استجابات إدارة المستخدمين تستخدم إسقاطاً صريحاً للحقول؛ اختبارات الأمن تمر. |
| SEC-02 | مكتمل محلياً لمسار Firebase وGoogle | أُلغي الربط اعتماداً على البريد فقط؛ الربط الصريح يتطلب جلسة وكلمة مرور وهوية Google موثقة، مع تحديث مشروط. اختبارات تعارض المعرّف والطالب والمدير والربط تمر. |
| SEC-03 | مكتمل محلياً | سر إعداد TOTP معلّق مستقل، وتفعيل مشروط بعد الرمز، واستعادة إدارية تتطلب كلمة مرور المدير وتأكيد البريد والسبب وتلغي جلسات صاحب الحساب. يجب تطبيق الهجرة `0009_totp_pending.sql` قبل نشر الكود. |
| AUTH-01 | منفذ محلياً ويحتاج تجربة متصفح | التسجيل يعرض انتظار التوثيق وإعادة الإرسال؛ العودة من الرابط تعرض النجاح أو الفشل. يلزم فحص الهاتف وقارئ الشاشة والبريد الحقيقي. |
| AUTH-02 | مكتمل محلياً | أُزيل منتقي Google المحلي والحسابات المخزنة المضللة؛ الزر يستدعي Firebase الحقيقي. |
| AUTH-03 | مكتمل محلياً | زر Google في لوحة الإدارة يعود لحالته عبر `finally` بعد إلغاء النافذة أو الخطأ. |
| AUTH-04 | يحتاج staging | مشروع Firebase وتطبيق الويب في حالة ACTIVE وفق القراءة من الوصلة، وملف `firebase.json` المحلي يسرد نطاق Worker. لم يثبت ذلك أن موفر Google والنطاقات مفعّلان في الإعداد المنشور أو أن الدخول ينجح بحساب حقيقي. |
| AUTH-05 | منفذ محلياً ويحتاج بريد حقيقي | إنشاء المستخدم الإداري يرسل دعوة لاختيار كلمة المرور؛ إنشاء الحساب وتعديله يرفضان كلمة مرور يحددها المدير. مسار الرابط يثبت البريد للحساب المدعو. عند فشل الإرسال يُلغى الحساب الجديد. |
| SEC-04 | غير مكتمل | ما زالت سمات أحداث مضمنة كثيرة في صفحتي HTML، لذلك لا يمكن إزالة `script-src-attr 'unsafe-inline'` بأمان. اتبع `CSP_MIGRATION_PLAN.md`. |
| UX-01 | مكتمل محلياً | أزرار الدخول التجريبي والمعاينة مخفية من شاشة الإدارة؛ استُبدل README القديم بتعليمات Worker الحالية. |
| UX-02 | جزئي | تظهر رسائل توثيق البريد وفشل تسليم OAuth بدلاً من الرجوع الصامت. يلزم فحص شامل لكل حالات الشبكة والجلسة والواجهة. |
| UX-03 | جزئي | أضيفت تسميات ثابتة لحقول الدخول وصُححت سياسة 10 أحرف وأُزيل رابط الشروط المعطل. تنتظر روابط الشروط والخصوصية نصوصاً معتمدة، ويلزم فحص الوصولية الكامل. |
| OPS-01 | جزئي | نسخة Worker الحالية تستجيب وفحص `/health/ready` يؤكد D1. الهجرة `0009` وحدها معلقة قبل النشر. أسرار Worker الحالية تقتصر على Firebase وJWT؛ `SMTP_HOST` و`SMTP_PASSWORD` فارغان، لذا تسجيل البريد ودعوات الموظفين واستعادة كلمة المرور غير متاحة إنتاجياً حتى يُضبط مزود بريد HTTP موثوق. يلزم تحقق حقيقي من Google والبريد وR2 وتدفقات الطالب والموظف. |

التحقق المحلي: `npm run typecheck`، و`npm run build` بصيغة dry run، و611 اختبار Vitest مستهدفاً لـ`src` (44 ملفاً). فُتحت شاشتا دخول الطالب والإدارة في Worker محلي على عرض 360px و1280px؛ ظهر تخطيط النماذج والتسميات، وجرى تصحيح نصوص التسجيل العربية. هذا فحص عرض أولي، ولا يغني عن دخول حقيقي أو تدقيق وصولية شامل. قبل تطبيق الهجرة سُجلت علامة استعادة D1؛ راجع سجل النشر في نهاية الملف.

## سجل النشر

- النسخة الإنتاجية قبل التحديث: `d619ef66-fef6-476d-b6de-881aa766316b`، وفحص D1 جاهز.
- علامة استعادة D1 قبل الهجرة: `0000005f-00000000-000050f2-3b215d5b9e3bcc5368292c7194a66796`.
- النشر وترحيل `0009` والتحقق اللاحق: يُحدّث هذا السطر بعد التنفيذ.
